# Use Node.js 22 image
FROM node:22

# Create and set the working directory
WORKDIR /workspace

# Default command is overridden by docker-compose; this keeps the container alive.
CMD ["sleep", "infinity"]